-
  hosts: all
  become: true
  tasks: 
    - name: discover OS
      command: 
        - . /etc/os-release
        - echo $ID
      register: os
    - name: install Nginx on Debian
      apt:
        name: nginx
        state: present
      when: "{{ os }}" == "Debian"

    - name: install Nginx on Rocky
      dnf:
        name: Nginx
        state: present
      when: "{{ os }}" == "Rocky"

...